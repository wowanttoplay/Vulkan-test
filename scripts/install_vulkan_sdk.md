# Vulkan SDK 安装指南

## 检测到：Vulkan SDK 未安装

Vulkan SDK 是开发Vulkan应用程序的必备工具，包含：
- Vulkan 头文件和库
- 验证层（调试用）
- **glslc** - GLSL着色器编译器（必需！）
- Vulkan配置工具
- 示例程序

---

## 快速安装步骤

### 方法1：自动下载（推荐）

我已经为你准备了下载脚本，运行：

```batch
download_vulkan_sdk.bat
```

这个脚本会：
1. 打开Vulkan SDK下载页面
2. 自动检测最新版本
3. 指导你完成安装

### 方法2：手动下载

#### 步骤1：访问下载页面
打开浏览器，访问：
```
https://vulkan.lunarg.com/sdk/home#windows
```

#### 步骤2：下载安装程序
- 点击 **"Download Latest SDK"** 按钮
- 或者点击 **"Windows"** 标签下的下载链接
- 文件名类似：`VulkanSDK-1.3.xxx.x-Installer.exe`
- 大小约：200-400 MB

#### 步骤3：运行安装程序
1. 双击下载的 `.exe` 文件
2. 点击 **"I Agree"** 接受许可协议
3. **重要：勾选所有组件**，特别是：
   - ✅ Vulkan SDK Core
   - ✅ Shader Toolchain (glslc)
   - ✅ Debugging Tools
   - ✅ Vulkan Configurator
4. 选择安装位置（推荐默认：`C:\VulkanSDK\1.3.xxx.x`）
5. 点击 **"Install"**
6. 等待安装完成（约2-5分钟）
7. **重要：勾选 "Add to PATH"**（自动设置环境变量）
8. 点击 **"Finish"**

#### 步骤4：验证安装
**重要：重新打开命令行窗口**（旧窗口不会识别新的环境变量）

运行验证脚本：
```batch
verify_vulkan_installation.bat
```

或手动验证：
```batch
# 检查环境变量
echo %VULKAN_SDK%
# 应该显示：C:\VulkanSDK\1.3.xxx.x

# 检查glslc
glslc --version
# 应该显示版本信息

# 检查Vulkan库
dir "%VULKAN_SDK%\Lib\vulkan-1.lib"
# 应该找到文件
```

---

## 常见问题排查

### ❌ 问题1："glslc 不是内部或外部命令"

**原因**：环境变量未正确设置

**解决方案A：重新运行安装程序**
1. 重新运行 `VulkanSDK-xxx-Installer.exe`
2. 选择 **"Modify"**
3. 确保勾选 **"Update PATH"**

**解决方案B：手动添加到PATH**
1. 按 `Win + R`，输入 `sysdm.cpl`，回车
2. 点击 **"高级"** 标签
3. 点击 **"环境变量"**
4. 在 **"系统变量"** 中找到 `Path`，点击 **"编辑"**
5. 点击 **"新建"**，添加：
   ```
   C:\VulkanSDK\1.3.xxx.x\Bin
   ```
   （替换 `1.3.xxx.x` 为你的实际版本号）
6. 点击 **"确定"** 保存
7. **重新打开命令行窗口**

### ❌ 问题2："%VULKAN_SDK% 返回空值"

**原因**：环境变量未设置

**解决方案：手动设置 VULKAN_SDK**
1. 按 `Win + R`，输入 `sysdm.cpl`，回车
2. 点击 **"高级"** → **"环境变量"**
3. 在 **"系统变量"** 中点击 **"新建"**
4. 变量名：`VULKAN_SDK`
5. 变量值：`C:\VulkanSDK\1.3.xxx.x`（你的安装路径）
6. 点击 **"确定"**
7. **重启电脑**（确保生效）

### ❌ 问题3："找不到 vulkan-1.lib"

**原因**：安装不完整

**解决方案**：
1. 重新运行安装程序
2. 选择 **"Repair"** 或 **"Reinstall"**
3. 确保勾选 **所有组件**

### ❌ 问题4：安装后仍然无法编译

**解决方案**：
1. **完全关闭所有命令行窗口**
2. 重新打开新的命令行窗口
3. 重新运行 `verify_vulkan_installation.bat`

如果还是不行，**重启电脑**。

---

## 安装成功标志

运行 `verify_vulkan_installation.bat` 后，你应该看到：

```
========================================
Vulkan SDK Installation Verification
========================================

[✓] VULKAN_SDK environment variable found
    Path: C:\VulkanSDK\1.3.xxx.x

[✓] glslc shader compiler found
    Version: glslc 1.3.xxx.x

[✓] Vulkan library found
    Path: C:\VulkanSDK\1.3.xxx.x\Lib\vulkan-1.lib

[✓] Vulkan headers found
    Path: C:\VulkanSDK\1.3.xxx.x\Include\vulkan\vulkan.h

========================================
SUCCESS! Vulkan SDK is properly installed!
========================================

You can now build the Vulkan project:
1. cd C:\Users\Administrator\source\repos\Vulkan-test
2. tools\compile_shaders.bat
3. mkdir build && cd build
4. cmake ..
5. cmake --build . --config Debug
```

---

## 下一步

✅ 安装完成后：

1. **验证安装**：
   ```batch
   verify_vulkan_installation.bat
   ```

2. **编译着色器**：
   ```batch
   tools\compile_shaders.bat
   ```

3. **配置项目**：
   ```batch
   mkdir build
   cd build
   cmake ..
   ```

4. **编译项目**：
   ```batch
   cmake --build . --config Debug
   ```

5. **运行程序**：
   ```batch
   Debug\VulkanSandbox.exe
   ```

---

## 需要帮助？

如果遇到任何问题，请：
1. 截图错误信息
2. 运行 `verify_vulkan_installation.bat` 并分享结果
3. 告诉我安装步骤中卡在哪里

祝安装顺利！🚀
